import Model, { attr, belongsTo } from '@ember-data/model';
import { computed } from '@ember/object';

export default Model.extend({
  //createdAt: attr('date'),
  //donor: attr(),
  //receiver: attr(),
  //sharedBy: attr(),
 // donor: belongsTo('organization',  { async: false, inverse: null }),
  //receiver: belongsTo('organization',  { async: false, inverse: null }),
  //sharedBy: belongsTo('user',  { async: false, inverse: null }),
});

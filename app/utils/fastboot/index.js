import isFastboot from './isFastboot';
import win from './stubs/window';
import doc from './stubs/document';
import { localStorage, sessionStorage, makeStorage } from './stubs/storage';

export {
  isFastboot,
  win,
  doc,
  localStorage,
  sessionStorage,
  makeStorage,
};

{{#travis-layout layoutName="layouts/center" class="owner"}}
<div class="owner-header-wrapper">
<header class="owner-header m-t-l">
  <div class="owner-avatar">
    {{user-avatar url=owner.avatar name=owner.name size=126}}
  </div>
  <div class="owner-info">
    <h1 class="owner-title">{{owner.name}} {{trial-expired-badge}}</h1>
    <p class="owner-handle">
      {{svg-jar 'icon-repooctocat' class="icon--l"}}
      <a href={{githubProfile}} title="{{owner.name}} on GitHub">
        <span class="label-align">{{owner.login}}</span></a></p>
  </div>
</header>
{{#if features.proVersion}}
  {{trial-expired-notice org=owner.name}}
{{/if}}
</div>

<div class="tabs--owner">
  <div class="tabbody">
    <section role="tabpanel">
      {{outlet}}
    </section>
  </div>
</div>
{{/travis-layout}}

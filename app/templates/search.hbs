<div class="wrapper {{if auth.signedOut 'centered'}}">
  {{top-bar}}

  {{flash-display}}

  <div class="wrapper-main">
    <div id="main" class="main" role="main">
      {{#if repo}}
        {{repository-layout repo=repo build=build job=job tab=tab}}
      {{else}}
        {{#if repositories.loadingData}}
          {{loading-indicator}}
        {{else}}
          {{missing-notice title="No repositories found"}}
        {{/if}}
      {{/if}}
    </div>
  </div>

  {{#if auth.signedIn}}
  {{#unless features.dashboard}}
    <aside id="left" class="{{unless auth.signedIn 'hidden'}}">
      {{repository-sidebar}}
    </aside>
  {{/unless}}
  {{/if}}
</div>

{{#travis-form as |form| }}
  {{#form.field
    value=query
    allowClear=true
    onChange=onSearch
    as |field|
  }}
    {{yield (hash
        searchField=(component field.input
          onKeyUp=onSearch
        )
      )
    }}
  {{/form.field}}
{{/travis-form}}

{{yield (hash
    isFiltering=isFiltering
    isLoading=isLoading
    form=(component 'repository-filter-form' onSearch=(perform search))
    query=query
    results=repositories
    computeName=computeName
  )
}}

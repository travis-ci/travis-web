{{yield (hash
    isFiltering=isFiltering
    isLoading=isLoading
    form=(component 'repository-filter-form' query=query onSearch=(perform search))
    query=query
    results=repositories
    computeName=computeName
  )
  }}

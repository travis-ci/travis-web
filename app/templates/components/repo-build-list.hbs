<LoadMore
  @dynamicQuery={{this.loader}}
  @liveItems={{this.builds}}
  @missingNotice={{@missingNotice}}
  as |loader|
>
  <ul class="build-list">
    {{#each loader.items as |build|}}
      <BuildsItem @build={{build}} />
    {{/each}}
  </ul>
</LoadMore>
<button
  data-test-request-configs-trigger-build="true"
  onclick={{this.onTriggerBuild}}
  type="button"
  class="request-configs-button-trigger-build button--blue"
  title="Trigger build"
>
  {{#if this.processing}}
    <LoadingIndicator />
  {{else}}
    Trigger build
  {{/if }}
</button>

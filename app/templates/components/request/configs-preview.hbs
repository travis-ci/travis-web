<Request::Messages
  @messages={{this.messages}}
/>

<div class="request-configs-preview-container">
  <div class="left">
    <h4>
      Build Config
    </h4>

    {{#if this.loading}}
      <LoadingIndicator />
    {{else}}
      <CodeBlock
        @language="json"
        @code="{{this.formattedRequestConfig}}"
        class="codedisplay line-numbers"
        data-test-preview-config="true"
      />
    {{/if}}
  </div>

  <div class="right">
    <h4>
      Build Matrix
    </h4>

    {{#if this.loading}}
      <LoadingIndicator />
    {{else}}
      <CodeBlock
        @language="json"
        @code="{{this.formattedJobConfigs}}"
        class="codedisplay line-numbers"
        data-test-preview-config="true"
      />
    {{/if}}
  </div>
</div>

{{#if routeModel}}
  <LinkTo @route={{routeName}} @model={{routeModel}} class={{class}}>
    {{yield}}
  </LinkTo>
{{else}}
  <LinkTo @route={{routeName}} class={{class}}>
    {{yield}}
  </LinkTo>
{{/if}}
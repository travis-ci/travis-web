{{#link-to 'owner' user.login}}
  {{tooltip-on-element text=(concat 'View owner page for ' user.name)}}
  {{user-avatar url=user.avatarUrl name=user.name small=true size=18}}
  <span class="label-align inner-underline">{{user.name}}</span>
{{/link-to}}

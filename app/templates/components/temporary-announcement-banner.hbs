{{#if this.enabled}}
  <div class="notice-banner--yellow align-center">
    {{this.message}}
    <button type="button" class="no-button close-banner" {{action this.closeBanner}}>
      &times;
    </button>
  </div>
{{/if}}
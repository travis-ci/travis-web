{{#if this.enabled}}
  <Notification
    @type='global'
    @severity='warning'
    data-test-temporary-announcement='true'
  >
    {{this.message}}

    <button type="button" class="no-button close-banner" {{action "closeBanner"}}>
      <SvgImage @name="flash-close" @class="icon-close" />
    </button>
  </Notification>
{{/if}}
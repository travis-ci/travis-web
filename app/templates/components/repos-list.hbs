{{#if repos}}
  <ul class="repos-list">
    {{#each repos as |repo|}}
      {{repos-list-item repo=repo}}
    {{/each}}
  </ul>
  {{else}}
    <div class="loading-container">{{noReposMessage}}</div>
{{/if}}

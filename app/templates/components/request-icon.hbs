{{#if isEmpty}}
  {{inline-svg 'images/stroke-icons/icon-nobuilds'}}
{{else}}
  {{#if isPush}}
    {{inline-svg 'images/stroke-icons/icon-push'}}
  {{/if}}

  {{#if isPR}}
    {{inline-svg 'images/stroke-icons/icon-pullrequest'}}
  {{/if}}

  {{#if isCron}}
    {{inline-svg 'images/stroke-icons/icon-cronjobs'}}
  {{/if}}

  {{#if isAPI}}
    {{inline-svg 'images/stroke-icons/icon-api'}}
  {{/if}}
{{/if}}

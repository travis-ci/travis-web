{{#if isEmpty}}
  {{inline-svg 'icon-nobuilds'}}
{{else}}
  {{#if isPush}}
    {{inline-svg 'icon-push'}}
  {{/if}}

  {{#if isPR}}
    {{inline-svg 'icon-pullrequest'}}
  {{/if}}

  {{#if isCron}}
    {{inline-svg 'icon-cronjobs'}}
  {{/if}}

  {{#if isAPI}}
    {{inline-svg 'icon-api'}}
  {{/if}}
{{/if}}

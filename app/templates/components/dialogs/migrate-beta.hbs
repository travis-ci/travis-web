<div class='migrate-beta-dialog'>

  <div class='migrate-beta-dialog__image'>
    {{svg-jar 'migrate-beta' width='311px'}}
  </div>

  <h3 class='h3--teal align-center'>
    Register for beta
  </h3>

  <p>
    Gain access to have all your public and private repositories together.
  </p>

  <p>
    Your account will be registered automatically once you apply. Also you can add any organizations.
  </p>

  <p>
    {{#travis-form as |form| }}
      {{#form.field
        label='Select your accounts'
        value=selectedAccounts
        multiple=true
        onChange=(action (mut selectedAccounts))
        as |field|
      }}
        {{#field.select
          placeholder='Search organizations'
          options=selectableAccounts
          searchField='title'
          onkeydown=(action 'preventErase')
          as |option|
        }}
          {{option.title}}
        {{/field.select}}
      {{/form.field}}
    {{/travis-form}}
  </p>

  <p class='subtle'>
    Once you've picked accounts you will be placed on the waitlist for beta!
    Make sure to check your organization pages for any updates &dash;
    we'll notify you there when we've enabled beta.
  </p>

  <div class='modal-controls'>
    <button
      class='button--blue {{if register.isRunning 'disabled'}}'
      disabled={{register.isRunning}}
      onclick={{perform register}}
    >
      {{#if register.isRunning }}
        {{loading-indicator white=true inline=true }} Registering...
      {{else}}
        Register account
        {{if (gt selectedAccounts.length 1) 'and organizations'}}
      {{/if}}
    </button>
  </div>

  <button
    type="button"
    title="Close modal"
    class="modal-close pointer"
    onclick={{action 'onClose'}}
  >
    {{svg-jar 'icon-failed' class="icon"}}
  </button>

</div>

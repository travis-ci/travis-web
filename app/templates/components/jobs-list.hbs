{{#if jobs.length}}

  {{#if required}}
    <h2 class="section-title">Build Jobs</h2>
  {{else}}
    <h2 class="section-title">Allowed Failures
      <span class="tooltip-jobs" data-tooltip="These are jobs you can allow to fail without failing your entire build">
        <span class="tooltip-trigger">{{inline-svg 'images/stroke-icons/icon-help' class='icon-help'}}</span>
      </span>
    </h2>
  {{/if}}

    <ul class="jobs-list">
    {{#each jobs as |job|}}
      {{jobs-item job=job repo=repo}}
    {{/each}}
    </ul>
{{/if}}

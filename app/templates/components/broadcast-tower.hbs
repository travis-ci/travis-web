<span class="icon-broadcast {{status}} {{if isOpen 'is-open'}}" title="broadcasts" {{action (perform toggle)}}>
  {{svg-jar 'icon-broadcast'}}
  {{#if count}}<div class='count-wrapper'><div class='count'>{{count}}</div></div>{{/if}}
</span>

<nav class="travistab-nav--secondary">
  <ul>
    <li>
      <LinkTo
        @route="job.index"
        @models={{array repo job}}
        data-test-job-log-tab
        @id="tab_log"
        @title="Look at this job's log"
      >
        Job log
      </LinkTo>
    </li>
    <li>
      <LinkTo
        @route="job.config"
        @models={{array repo job}}
        data-test-build-yaml-tab
        @title="Look at this job’s config"
        @disabled={{job.build.request.noYaml}}
      >
        {{#if job.build.request.noYaml}}
          <EmberTooltip @text="Try a more recent job to see the config" />
        {{/if}}
        View config
      </LinkTo>
    </li>
  </ul>
</nav>
<div class="travistab-body job-tabs">
  {{outlet}}
</div>
<ul data-test-components-flash-item="true" class="flash ember-view">
  <li class="warning below-top-bar ember-view">
    <p class="flash-message">
      <span data-test-flash-message-text class="message">
        Builds have been temporarily disabled for public repositories due to a negative credit balance.
        Please go to the 
        {{#if @data.isUser}}
          <LinkTo @route="account.billing">Plan page</LinkTo>
        {{else}}
          <LinkTo @route="organization.billing" @model={{@data.owner}}>Plan page</LinkTo>
        {{/if}}
        to replenish your credit balance or alter your 
        {{#if @data.isUser}}
          <LinkTo @route="account.settings" @query={{hash anchor="oss-consumption"}}>OSS Credits consumption setting</LinkTo>.
        {{else}}
          <LinkTo @route="organization.settings" @model={{@data.owner}} @query={{hash anchor="oss-consumption"}}>OSS Credits consumption setting</LinkTo>.
        {{/if}}
      </span>
    </p>
  </li>
</ul>
<LoadingOverlay @visible={{isLoading}}>
  {{#each topicsToShow as |topic|}}
    <li>
      <ExternalLinkTo @href={{topic.url}}>
        {{topic.title}}
      </ExternalLinkTo>
    </li>
  {{/each}}
</LoadingOverlay>
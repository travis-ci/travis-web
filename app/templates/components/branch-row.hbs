<div class="two-line fade-out">
  <div class="row-name">
    <h2 class="row-item">{{status-icon status=buildState}}
    <span class="label-align">{{branch.name}}</span></h2>
  </div>
  <div class="row-builds">
    <div class="row-item">
    <span class="icon icon-line-build"></span>
    <span class="label-align">
    {{#if branch.current_build}}
      {{#if getLast5Builds.isLoading}}
        {{loading-indicator inline=true}}
      {{else}}
        {{getLast5Builds.count}} builds
      {{/if}}
    {{else}}
      0 builds
    {{/if}}</span>
    </div>
  </div>
</div>
<div class="two-line row-last-build fade-out">
  <div class="row-request">
    <div class="row-item">
      {{#if branch.current_build}}
        {{request-icon event=eventType state=buildState}}
        {{#link-to "build" branch.current_build.id}}
          <span class="label-align">#{{branch.current_build.number}} {{buildState}}</span>
        {{/link-to}}
      {{else}}
        {{request-icon event=eventType state=buildState}} -
      {{/if}}
    </div>
  </div>
  <div class="row-calendar">
    <div class="row-item" title="{{pretty-date branch.current_build.finished_at}}">
      <span class="icon-calendar"></span>
      <span class="label-align">
      {{#if branch.current_build}}
        {{format-time branch.current_build.finished_at}}
      {{else}}
        -
      {{/if}}</span>
      </div>
  </div>
</div>
<div class="two-line">
  <div class="row-commit">
    <div class="row-item">
      <span class="icon-github"></span>
      <a href="{{urlGithubCommit}}" title="commit on GitHub">
      <span class="label-align">
      {{#if branch.current_build}}
        {{format-sha branch.current_build.commit.sha}}
      {{else}}
        -
      {{/if}}</span>
      </a></div>
  </div>
  <div class="row-commiter">
    <div class="row-item">
      {{#if branch.current_build}}
        <img src="{{branch.current_build.commit.committer.avatar_url}}" alt="avatar" class="avatar--small"><span class="label-align">{{branch.current_build.commit.committer.name}}</span>
      {{else}}
        <div class="avatar--small"></div><span class="label-align">no commits yet</span>
      {{/if}}
    </div>
  </div>
</div>
<div class="one-line is-relative">
  <ul class="build-tiles">
  {{#if getLast5Builds.isLoading}}
    {{loading-indicator center=true}}
  {{else}}
    {{#each  getLast5Builds.content as |buildTile|}}
      {{build-tile build=buildTile}}
    {{/each}}
  {{/if}}
  </ul>
</div>

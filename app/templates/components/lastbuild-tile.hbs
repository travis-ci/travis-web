<LinkTo
  @route="build"
  @models={{array
    this.repo.provider
    this.repo.vcsLogin
    this.repo.vcsName
    build.id
  }}
>
  <StatusIcon @status={{this.build.state}} />
  <span class="label-align">
    #{{this.build.number}}
  </span>
</LinkTo>

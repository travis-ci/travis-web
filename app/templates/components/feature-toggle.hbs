{{#if toggleFeatureTask.isRunning}}
  {{loading-indicator}}
{{else}}
  <div class="switch-inner">
    <span class="on">
      ON
    </span>
    <span class="off">
      OFF
    </span>
  </div>
{{/if}}

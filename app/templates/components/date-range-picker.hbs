{{#if hasBlock}}
  {{yield}}
{{else}}
  {{#if label}}
    <label class={{labelClass}}>{{label}}</label>
  {{/if}}
  <div class="month-picker-label daterangepicker-input form-control">
          <span>{{this.rangeText}}</span>
          <span class="caret down"></span>
  </div>
{{/if}}
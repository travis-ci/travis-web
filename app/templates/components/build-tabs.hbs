<nav class="travistab-nav--secondary">
  <ul>
    <li>{{link-to 'Build matrix?' 'build.index' build.repo build data-test-build-matrix-tab title="Look at this build matrix"}}</li>
    <li>
      {{#link-to 'build.config' build.repo build data-test-build-yaml-tab title="Look at this build's config"}}
        View config

        {{#if build.request.messagesRequest.messages.length}}
          <span data-test-yaml-badge class='badge--yaml'>
            <span class='count'>{{build.request.messagesRequest.messages.length}}</span>
          </span>
        {{/if}}
      {{/link-to}}
    </li>
  </ul>
</nav>
<div class="travistab-body">

  {{outlet}}

</div>

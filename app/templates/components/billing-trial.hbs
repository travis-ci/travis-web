{{#unless model.subscriptions}}
  <h2>Plan Overview</h2>
  {{#if model.trials}}
    {{#if model.trials.hasActiveTrial}}
      <p data-test-trial-message class="source plan-status-message">You've got {{pluralize model.trials.buildsRemaining 'trial build'}} left.<br /> Ensure unlimited builds by setting up a plan before it runs out!</p>
    {{else}}
      <p data-test-trial-message class="source plan-status-message">Your trial has just ended.<br /> To get the most out of Travis CI, set up a plan below!</p>
    {{/if}}
  {{else}}
    <p data-test-trial-message class="source plan-status-message"> Your trial includes 100 trial builds and 2-concurrent-jobs, no credit card required. <br /> Need help? <a data-test-trial-link href={{config.urls.gettingStarted}} class="billing-section-link">Check our getting started guide</a>.</p>
  {{/if}}
  {{#if model.subscriptions.isGithub}}
    <a href={{config.marketplaceEndpoint}} class='button--blue marketplace-button'>Manage on GitHub Marketplace</a>
  {{else}}
    {{new-subscription-button trial=model.trials account=account}}
  {{/if}}
{{/unless}}
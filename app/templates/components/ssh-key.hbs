<div class="ssh-key-name">
  {{inline-svg 'images/stroke-icons/icon-key' class="icon"}}
  <span>{{if key.isCustom key.description 'no custom key set'}}</span>
</div>
<div class="ssh-key-value">
  {{inline-svg 'images/stroke-icons/icon-fingerprint' class="icon"}}
  <span>{{key.fingerprint}}</span>
</div>

{{#if key.isCustom}}
  <div class="ssh-key-action">
    {{#if delete.isRunning}}
      {{loading-indicator}}
    {{else}}
      {{#if pushAccess}}
        <div class="tooltip">
          <button {{action (perform delete)}} type="button" class="no-button ssh-delete">
            {{inline-svg 'images/stroke-icons/icon-trash' class="icon-trash"}}
          </button>
          <div class="tooltip-bubble">Delete</div>
        </div>
      {{else}}
        <div class="tooltip--height">
          {{inline-svg 'images/stroke-icons/icon-trash-disabled' class="icon ssh-no-delete"}}
          <div class="tooltip-bubble">You can't<br>delete keys</div>
        </div>
      {{/if}}
    {{/if}}
  </div>
{{else}}
  <div class="ssh-key-action">
    <div class="tooltip--height">
      {{inline-svg 'images/stroke-icons/icon-trash-disabled' class="icon ssh-no-delete"}}
      <div class="tooltip-bubble">Default keys <br>cannot be deleted</div>
    </div>
  </div>
{{/if}}

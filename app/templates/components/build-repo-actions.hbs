{{#if canCancel}}
  {{#if cancelling}}
    {{loading-indicator height=true}}
  {{else}}
    <a {{action "cancel"}} class="action-button--cancel" title="Cancel build">
      Cancel build
    </a>
  {{/if}}
{{/if}}

{{#if canRestart}}
  {{#if restarting}}
    {{loading-indicator height=true}}
  {{else}}
    <a {{action "restart"}} class="action-button--restart" title="Restart build">
      Restart build
    </a>
  {{/if}}
{{/if}}

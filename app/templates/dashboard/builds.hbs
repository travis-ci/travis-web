<ul class="repo-list">
  {{#each (sort-by "startedAt:desc" this.model) as |build|}}
    <MyBuild data-test-my-build @build={{build}} />
  {{else}}
    <NoBuilds 
      @title="No builds started yet"
      @subtitle="Want to start testing on Travis CI?"
    />
  {{/each}}
</ul>

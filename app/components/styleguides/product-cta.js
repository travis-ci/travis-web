import Component from '@ember/component';

export default Component.extend({
  classNames: [
    'shadow-even-md',
    'rounded',
    'flex',
    'flex-wrap',
    'overflow-hidden',
  ],

  products: null,
});

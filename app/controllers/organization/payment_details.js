import Controller from '@ember/controller';
import PaymentDetailsControllerMixin from 'travis/mixins/controller/payment_details';

export default Controller.extend(PaymentDetailsControllerMixin, {});

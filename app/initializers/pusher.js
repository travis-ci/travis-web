let PusherInitializer, initialize;

initialize = () => null;

PusherInitializer = {
  name: 'pusher',
  after: 'ember-data',
  initialize
};

export { initialize };

export default PusherInitializer;

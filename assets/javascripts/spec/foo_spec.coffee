describe 'Foo', ->
  it 'bar', ->
    link = $($('#repositories a.slug')[0])
    console.log $('body').html()
    # link.attr('href').should.equal '#/travis-ci/travis-core'

  it 'bar', ->
    link = $($('#repositories a.slug')[0])
    # link.attr('href').should.equal '#/travis-ci/travis-core'

